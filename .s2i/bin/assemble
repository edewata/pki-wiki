#!/bin/bash

. ${STI_SCRIPTS_PATH}/assemble

# create redirection
cp ${HOME}/.s2i/etc/conf.d/99-redirect.conf /opt/app-root/etc/conf.d/

# create links
mv ${HOME}/images ${HOME}/images.orig
ln -s /opt/app-root/data/images ${HOME}/images
ln -s /opt/app-root/data/pki ${HOME}/pki
